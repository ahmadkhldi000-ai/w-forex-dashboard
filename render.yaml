services:
  - type: web
    name: w-forex-vip-dashboard
    env: node
    region: oregon
    plan: free
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    healthCheckPath: /login
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: COOKIE_SECURE
        value: 'true'
      # Auth token for the MT5 EA
      - key: AUTH_TOKEN
        generateValue: true
      # Session secret
      - key: SESSION_SECRET
        generateValue: true
      # Telegram (optional — set in Render dashboard)
      - key: TELEGRAM_TOKEN
        sync: false
      - key: TELEGRAM_CHAT
        sync: false
      # Private Telegram channel invite link (shown in the dashboard popup)
      - key: TELEGRAM_CHANNEL_LINK
        value: 'https://t.me/+iXalBkHABfBkYWQ0'
      # Google OAuth (optional — set in Render dashboard)
      - key: GOOGLE_CLIENT_ID
        sync: false
      - key: GOOGLE_CLIENT_SECRET
        sync: false
      - key: GOOGLE_REDIRECT_URI
        value: 'https://w-forex-dashboard.onrender.com/api/auth/google/callback'
      # Owner account (auto-seeded on boot with these credentials)
      - key: OWNER_EMAIL
        value: 'ahmadkhldi000@gmail.com'
      - key: OWNER_NAME
        value: 'Ahmad Khaldi'
      - key: OWNER_PASSWORD
        value: 'Wforex2026!'
      # SIM mode — keep ON for demo without EA
      - key: SIM_MODE
        value: 'on'
